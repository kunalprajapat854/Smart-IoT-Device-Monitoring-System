server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/iot_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9092
  application:
    name: iot-monitoring-system