server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/iot_db
    username: root
    password: Kunal@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9092
  application:
    name: iot-monitoring-system